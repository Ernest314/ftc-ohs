task main()
{
    SetSensor(IN_1, SENSOR_TOUCH);
    SetSensor(IN_2, SENSOR_TOUCH);
    SetSensorLight(IN_3);
    SetSensorLowspeed(IN_4);

    OnFwd(OUT_BC, 75);
    while(true)
    {
        if( SensorUS(IN_4) < 15 )
        {
            Float(OUT_BC);
        }
    }
    Float(OUT_ABC);
}
